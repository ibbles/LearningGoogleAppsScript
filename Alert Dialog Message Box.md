We can display message boxes with
```js
SpreadsheetApp.getUi().alert("Formulas and values are not the same size, cannot continue.");
```

Script execution will pause until the user clicks the OK button.